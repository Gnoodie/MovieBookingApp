rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Cho phép đọc/ghi toàn bộ database để thuận tiện test
      // Trước khi release lên Production thì sẽ phải khóa lại
      allow read, write: if true;
    }
  }
}
